# Moved to https://codeberg.org/snaily/versatile

### Migrating
Commands as root:
1. `eselect repository remove versatile` (use -f if prompted to force)
2. `eselect repository add versatile git https://codeberg.org/snaily/versatile`
3. `emerge --sync` or `emaint sync -r gentoo`
